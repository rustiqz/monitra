//! `Notifier` provider: webhook (DESIGN.md §4.1, Appendix — "default build").
//!
//! Compiled into the default build (not feature-gated). NOTE: §4.1 names
//! "log sink" as the default `Notifier`, but no separate log-sink crate
//! exists in the Appendix — this discrepancy is flagged in the Phase 1
//! brief and needs resolving before Phase 7 implements notifier behavior.
//!
//! Empty as of Phase 1 (scaffolding).
